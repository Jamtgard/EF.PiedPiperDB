package com.example.piedpiperdb.View;

import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.layout.AnchorPane;
import javafx.stage.Stage;

//GEFP-21-SJ
public class TeamView extends AbstractScene{

    /*
    public static Scene teamScene(Stage window){
        Scene teamBaseScene = AbstractScene.getScene(window);

        Button createNewTeam = new Button("Create New Team");
        Button showAllTeams = new Button("Show All Teams");
        Button getTeamById = new Button("Get Team By ID");
        Button updateTeam = new Button("Update Team");
        Button deleteTeamById = new Button("Delete Team by ID");

        AnchorPane teamPane = (AnchorPane) teamBaseScene.getRoot();
        teamPane.getChildren().addAll(createNewTeam, showAllTeams, getTeamById);

        return teamBaseScene;
    }

     */
}
